import 'package:flutter/cupertino.dart';

class CandidatesViewScreen extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Onboarding View Content"),
    );
  }
}